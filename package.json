{
  "name": "netflix-clone",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm --prefix Backend run start\" \"npm --prefix Frontend run dev -- --host 0.0.0.0\"",
    "backend": "npm --prefix Backend run start",
    "frontend": "npm --prefix Frontend run dev -- --host 0.0.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
